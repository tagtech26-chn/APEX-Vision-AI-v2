# Runtime dependencies. Version ranges are intentionally capped before the next
# breaking major release; upgrades should be validated through CI first.

# Core
fastapi>=0.141.1,<1.0
uvicorn>=0.52.1,<1.0
python-multipart>=0.0.32,<0.1
pydantic>=2.13.4,<3.0

# Image processing
opencv-python>=5.0.0.93,<6.0
numpy>=2.4.6,<3.0
pillow>=12.3.0,<13.0

# AI / ML
# Keep the PyTorch pair aligned; 2.6.x is the validated production baseline.
torch>=2.6.0,<2.7.0
torchvision>=0.21.0,<0.22.0
transformers>=4.39.3,<5.0
segment-anything>=1.0,<2.0
supervision>=0.29.1,<1.0
ultralytics>=8.4.115,<9.0
hydra-core>=1.3.4,<2.0

matplotlib>=3.11.1,<4.0
