# APEX Vision AI - production runtime dependencies (pinned to tested versions)
# Install with:
#   1) pip install -r requirements-prod.txt
#   2) pip install -r requirements-ai-source.txt   (after step 1, needs git + build tools)
#
# NOTE: the heavy AI pipeline additionally needs the Depth-Anything-V2 source
# clone. Point the *ROOT/*CKPT env vars at them on the target machine:
#   GROUNDING_DINO_CONFIG, GROUNDING_DINO_CKPT
#   SAM2_CONFIG_DIR, SAM2_CKPT
#   DEPTH_ANYTHING_ROOT, DEPTH_ANYTHING_CKPT
# Depth-Anything-V2 is loaded from DEPTH_ANYTHING_ROOT via sys.path (see
# app/ai/depth/depth_anything.py) - clone it and download depth_anything_v2_vitl.pth.
# Without the heavy models, set APEX_AI_PROVIDER=light to use the OpenCV pipeline.

# --- Web / API ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32
pydantic==2.13.4
httpx==0.28.1

# --- Image processing ---
# Headless OpenCV is required for the server/container runtime; it avoids GUI/X11
# shared-library dependencies such as libxcb that are not needed by the API.
opencv-python-headless==5.0.0.93
numpy==2.4.6
pillow==12.3.0
scipy==1.17.1

# --- AI / ML core ---
torch==2.13.0
torchvision==0.28.0
transformers==4.39.3
safetensors==0.8.0
sentencepiece==0.2.2
timm==1.0.28
accelerate==1.14.0

# --- Heavy provider stack (pip-installable parts) ---
supervision==0.29.1
ultralytics==8.4.115
hydra-core==1.3.4
omegaconf==2.3.1
pycocotools==2.0.11
einops==0.8.2
addict==2.4.0
iopath==0.1.10
yapf==0.43.0
