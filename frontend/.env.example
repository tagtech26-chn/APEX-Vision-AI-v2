# API base URL baked into the build. Leave empty for same-origin
# (single-service production where the backend serves the SPA).
# For dev against a separate backend: VITE_API_BASE=http://127.0.0.1:8000
VITE_API_BASE=
